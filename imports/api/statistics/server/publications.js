//import { Meteor } from 'meteor/meteor';
//import { Counts } from 'meteor/tmeasday:publish-counts';
//import { VedgeFlows } from '../vedge_flows.js';

/*
Meteor.publish('vedge_flows?L2-graph-data', function (graphParams) {
  console.log('server subscribe: vedge_flows?L2-graph-data');
  return VedgeFlows.find({
    environment: graphParams.env,
    object_id: graphParams.object_id,
    flowType: graphParams.flowType,
    sourceMacAddress: graphParams.sourceMacAddress,
    destinationMacAddress: graphParams.destinationMacAddress
  });
});

Meteor.publish('vedge_flows?L3-graph-data', function (graphParams) {
  console.log('server subscribe: vedge_flows?L3-graph-data');
  return VedgeFlows.find({
    environment: graphParams.env,
    object_id: graphParams.object_id,
    flowType: graphParams.flowType,
    sourceIPv4Address: graphParams.sourceIPv4Address,
    destinationIPv4Address: graphParams.destinationIPv4Address
  });
});
*/
